#!/usr/bin/env sh

# Remaps CapsLock to Escape because vim
# Press both Shift keys for CapsLock
# Control-Alt-Backspace restarts the shell in case gnome freezes up

# use 'localectl list-x11-keymap-options' to see all options
if is-supported setxkbmap; then
    setxkbmap -option \
        -option terminate:ctrl_alt_bksp \
        -option caps:swapescape \
        -option shift:both_capslock
else
    echo "Error: missing a required dependency: setxkbmap"
    exit 1
fi
