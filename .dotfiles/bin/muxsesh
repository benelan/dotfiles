#!/usr/bin/env bash
set -e

selected_path="${1:-"$(find ~/dev/work ~/dev/personal -maxdepth 1 -mindepth 1 -type d | fzf --cycle -0 -1)"}"
[[ -z "$selected_path" ]] || [[ ! -d "$selected_path" ]] && selected_path="$PWD"
tat "$selected_path"
unset selected_path
