#!/usr/bin/env bash

session_name="$(tmux display-message -p "#S")"
task_name="$(echo "$1" | tr "./" "__")"
target="$session_name:$task_name"

if ! tmux has-session -t "$target" 2> /dev/null; then
    tmux neww -dn "$task_name"
fi

shift
tmux send-keys -t "$target" "$*" Enter

